apiVersion: v1
kind: Service
metadata:
  name: my-service
  labels:
    app: my-app
spec:
  selector:
    app: nginx-pod
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 80
  type: ClusterIP
